import {View, Text} from 'react-native';
import React from 'react';

const ItemFullDescription = ({route}) => {
  console.log(route);

  return (
    <View>
      <Text>ItemFullDescription</Text>
    </View>
  );
};

export default ItemFullDescription;
